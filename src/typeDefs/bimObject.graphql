type BimObject {
    id: ID!
    name: String!
    models: [BimObjectModel!]!
    classificationNodes: [ClassificationNode!]!
}

input BimObjectFilter {
  classificationNodeId: ID
  softwareId: ID
}

